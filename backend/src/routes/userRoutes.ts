import { Router } from 'express';

const router = Router();

// Placeholders for user routes
// router.get('/', userController.getAllUsers);
// router.get('/:id', userController.getUserById);

export default router;
